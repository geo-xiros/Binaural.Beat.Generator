<mah:MetroWindow x:Class="Binaural.Beat.WpfClient.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 Title="Binaural Beat Player" 
                 ResizeMode="CanResizeWithGrip"
                 Height="460"
                 Width="560"
                 MinHeight="420"
                 MinWidth="520"
                 WindowStartupLocation="CenterScreen"
                 mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Real-Time Binaural Beat Player"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />

        <TextBlock Grid.Row="1"
                   Text="Based on binaural beat frequency presets"
                   Foreground="Gray"
                   Margin="0,0,0,14" />

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Target State" FontWeight="SemiBold" Margin="0,0,0,6" />
            <ComboBox x:Name="PresetComboBox"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Choice"
                      IsEditable="False"
                      MinHeight="30" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Duration (seconds)" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox x:Name="DurationTextBox"
                     Text="30"
                     MinHeight="30" />
        </StackPanel>

        <GroupBox Grid.Row="4" Header="Session" Margin="0,0,0,12">
            <StackPanel Margin="10">
                <TextBlock x:Name="SessionNameTextBlock" Text="Session: -" Margin="0,0,0,4" />
                <TextBlock x:Name="LeftFrequencyTextBlock" Text="Left ear: -" Margin="0,0,0,4" />
                <TextBlock x:Name="RightFrequencyTextBlock" Text="Right ear: -" />
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="5">
            <TextBlock x:Name="StatusTextBlock"
                       Text="Ready"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8" />
            <ProgressBar x:Name="PlaybackProgressBar"
                         Minimum="0"
                         Maximum="100"
                         Height="18" />
        </StackPanel>

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="PlayButton"
                    Content="Play"
                    Width="100"
                    Height="34"
                    Margin="0,0,8,0"
                    Click="PlayButton_Click" />
            <Button x:Name="StopButton"
                    Content="Stop"
                    Width="100"
                    Height="34"
                    IsEnabled="False"
                    Click="StopButton_Click" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
